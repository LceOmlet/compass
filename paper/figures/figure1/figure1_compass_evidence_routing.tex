% Figure 1 -- standalone wrapper.  Canvas is exactly \linewidth (397.485pt),
% so the exported PDF is placed 1:1 by main.tex and label point sizes on the
% page equal the point sizes in the source.
%   PDF : pdflatex figure1_compass_evidence_routing.tex
%   SVG : latex figure1_compass_evidence_routing.tex
%         dvisvgm --font-format=woff2 --exact --bbox=papersize \
%                 figure1_compass_evidence_routing.dvi
%   PNG : pdftoppm -png -r 600 -singlefile figure1_compass_evidence_routing.pdf \
%                 figure1_compass_evidence_routing
\documentclass[tikz,border=0pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{newtxtext,newtxmath}
\usepackage{xcolor}
\usetikzlibrary{arrows.meta,positioning,fit,calc,decorations.pathreplacing,patterns}

% Times-family text and matching mathematics; use natural script sizes.

\begin{document}
\input{figure1_compass_evidence_routing_body.tex}
\end{document}