# Remote snapshot: 2026-07-28

## Source boundary

The shared `reflection-bridge` directory was not a Git repository. Its total
size was about 987 MB:

- custom `bridge/`: about 1.2 MB;
- `experiments/`: about 236 KB;
- `runs/`: about 265 MB;
- all cache directories: the remaining majority.

Only custom source/configuration is tracked directly. Official semantic owners
are pinned as submodules and their observed working-tree changes are stored as
patches. Python bytecode, caches, model weights, API credentials, and generated
run state are excluded from Git.

## v16 stopping point

At inspection time on the replacement SSH endpoint (port 30915), there was no
process whose command line matched
`11_ifbench_siliconflow_sparse_single_gpu_v16.json`, and `nvidia-smi` reported
no compute process for it.

The persisted run contained:

- 93 `run_log.json` entries (`i=0` through `i=92`);
- a seven-skill pool with six accepted children;
- tree `0→1→2→3→4→5` and branch `0→6`;
- latest persisted proposer set `[4,5,6]`;
- `gepa_state.bin` last written at 2026-07-24 23:32 CST;
- launch output continuing into iteration 94 and ending mid-evaluation at
  2026-07-24 23:36 CST;
- no final-result file.

Therefore this snapshot is an interrupted stopping point, not evidence of
normal completion. It was not restarted or modified.

The full run stopping point is stored locally under `.local-runs/` and is
intentionally ignored by Git. File hashes are recorded in
`v16-local-run-manifest.json`.
